# 项目说明：当前社媒监控项目主要基于 Node.js 开发
# 核心依赖（如 whatsapp-web.js, pm2, express, sqlite3 等）已经记录在 social-monitor 目录下的 package.json 中。

# 这个 requirements.txt 通常用于记录 Python 项目的依赖。
# 如果你计划在后续引入 Python 辅助脚本（例如用于特定的 AI 数据预处理、大模型微调、或者数据清洗），
# 可以像下面这样在此处添加相关的依赖库：

# --- Python 依赖示例 ---
# python-dotenv==1.0.1
# requests==2.31.0
# openai==1.14.0
# pandas==2.2.1
# sqlite3
