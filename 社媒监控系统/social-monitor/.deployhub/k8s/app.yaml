apiVersion: v1
kind: ServiceAccount
metadata:
  name: social-monitor
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: social-monitor-wa-orchestrator
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "list", "watch", "patch"]
  - apiGroups: ["apps"]
    resources: ["deployments/scale"]
    verbs: ["get", "patch", "update"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: social-monitor-wa-orchestrator
subjects:
  - kind: ServiceAccount
    name: social-monitor
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: social-monitor-wa-orchestrator
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: social-monitor-data
spec:
  storageClassName: nas
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: social-monitor
  labels:
    app: social-monitor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: social-monitor
  template:
    metadata:
      labels:
        app: social-monitor
    spec:
      serviceAccountName: social-monitor
      containers:
        - name: social-monitor
          image: __IMAGE__
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http
          startupProbe:
            httpGet:
              path: /healthz
              port: http
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          env:
            - name: NODE_ENV
              value: production
            - name: DATA_DIR
              value: /data
            - name: PORT
              value: "3000"
            - name: DISABLE_MEDIA_UPLOAD
              value: "1"
            - name: PURGE_MEDIA_ON_START
              value: "1"
            - name: DB_DEGRADED_BOOT
              value: "1"
            - name: SKIP_CHROME_INSTALL
              value: "true"
            - name: PUPPETEER_SKIP_DOWNLOAD
              value: "true"
            - name: SSO_ENABLED
              value: "true"
            - name: SSO_LOGIN_URL
              value: "https://skyline-ark-sso.tyhark.com/login"
            - name: SSO_REDIRECT_PARAM
              value: "redirect"
            - name: SSO_USERINFO_URL
              value: "https://skyline-ark-sso.tyhark.com/token/userinfo"
            - name: COLLECTOR_TOKEN
              valueFrom:
                secretKeyRef:
                  name: social-monitor-secrets
                  key: COLLECTOR_TOKEN
            - name: STORAGE_MIN_FREE_MB
              value: "512"
            - name: STORAGE_MIN_FREE_PERCENT
              value: "5"
          envFrom:
            - secretRef:
                name: social-monitor-secrets
                optional: true
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 3Gi
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: social-monitor-data
---
apiVersion: v1
kind: Service
metadata:
  name: social-monitor
spec:
  selector:
    app: social-monitor
  ports:
    - name: http
      port: 3000
      targetPort: 3000
