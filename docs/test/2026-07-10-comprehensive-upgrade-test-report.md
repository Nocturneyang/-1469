# 安全、可靠性与三列客户资料栏测试报告

日期：2026-07-10

## 自动化结果

- `npm test`：通过。
- `npm run build`：通过；主入口 JS 为 339.19KB，gzip 114.06KB；异步管理页最大 gzip 12.26KB。
- `npm ls`：通过，无缺失或无效依赖。
- `npm audit --omit=dev`：0 vulnerabilities。
- `node scripts/predeploy-check.js --skip-install`：通过，包含路径边界和 `.deploy-worktree` 一致性校验。
- SQLite 在线备份：4 个全局库备份并通过 `quick_check`。
- SQLite 恢复演练：4 个库恢复到临时目录并通过 `integrity_check`，未切换线上文件。

自动化覆盖停用坐席、入口显式否决、范围撤销、只读坐席备注、账号发送默认关闭、客户类型筛选与停用、FloodWait、dead 状态、WA delivered 回执、Worker 平台容量上限、幂等、串行、熔断和重启恢复。

## 浏览器响应式结果

- 1440×900：服务账号导航 240px，会话/消息/资料列为 320/560/320px，无横向溢出。
- 1280×900：导航压缩为 82px，三列为 300/612/286px，无横向溢出。
- 1179×900：资料栏默认收起，打开后为右侧 380px 固定抽屉，带遮罩，无横向溢出。
- 390×844：资料抽屉覆盖完整可用内容宽度，无横向溢出；修复了 `100vw` 与滚动条叠加导致的左偏问题。

## 未执行项

未使用生产 WA/TG 账号进行真实发送。真实渠道端到端收发、WA ACK 和引用只允许在测试账号、全局与账号发送开关均由管理员显式开启、限速和熔断生效的灰度环境继续验证。
