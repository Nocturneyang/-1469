# TG 实时会话与媒体测试报告

日期：2026-07-14

## 验证范围

- TG photo/document 媒体描述与 MIME。
- 历史二进制媒体的 JPEG/PNG/GIF/WebP 文件头识别。
- TG 群名、发送人、用户名、引用、转发和查看数元数据。
- raw message `updated_at` 迁移和 upsert。
- 会话 SSE 认证、权限检查和 ready 首包。
- 前端实时订阅、历史合并、图片预览与元数据展示。
- 生产前端构建。

## 结果

| 项目 | 命令 | 结果 |
| --- | --- | --- |
| Node 语法 | `node --check` 核心变更文件 | 通过 |
| 全量自动化 | `npm test` | 通过 |
| 前端构建 | `npm run build` | 通过 |
| 浏览器渲染 | 应用内浏览器连接 | 未执行：桌面浏览器运行时初始化失败 |

`npm run build` 仅输出上游 `@vueuse/core` pure annotation 告警，构建退出码为 0，与本次改动无关。

## 结论

实时通知、消息幂等合并、TG 图片 MIME 和完整元数据均已由自动化测试覆盖。发布前仍建议在浏览器运行时恢复后，用测试 TG 账号发送一条文本和一张图片做最终冒烟。
