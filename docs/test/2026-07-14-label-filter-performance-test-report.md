# 标签筛选性能测试报告

日期：2026-07-14

## 覆盖

- 标签选择后的前端即时过滤与精确缓存。
- 旧请求失效，防止过期群列表回包。
- raw 群列按指定 group ID 查询。
- 同步群和标签映射的 API 筛选。
- 平台和服务账号权限隔离。

## 自动化结果

| 项目 | 结果 |
| --- | --- |
| CommonJS 语法检查 | 通过 |
| `npm test` | 通过 |
| `npm run build` | 通过 |

测试额外构造 90 个更新的无关群，并验证指定的旧目标群仍能通过 group ID 定向查询返回，不受默认最新列表限制影响。

## 渲染验收

应用内浏览器运行时在当前桌面环境中无法初始化，因此未执行登录态截图和真实交互计时。已使用组件契约、API 集成测试和生产构建覆盖。

